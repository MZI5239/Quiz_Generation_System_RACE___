# RACE RC & Quiz Generation System: Master Context for AI Agents

**Target Audience:** Any LLM or AI agent continuing work on this project.

## 1. Project Overview
This project implements an Intelligent Reading Comprehension and Quiz Generation System based on the **RACE Dataset**. It strictly uses **Traditional Machine Learning** models (no Deep Learning/Neural Networks like BERT/T5 allowed) to handle text generation, option verification, and distractor/hint creation.

## 2. Completed Architecture & Flow
The project is fully structured and implemented based on the requirements.
* **Data Flow**: `train.csv` -> `preprocessing.py` -> `Model A` & `Model B` -> `inference.py` -> `ui/app.py`
* **Splits**: The data is strictly loaded from a single file (`train.csv`) and split 80/10/10.

### Model A: Question Generation & Answer Verification
* **Models Used**: Logistic Regression, SVM (LinearSVC), Naive Bayes, Soft-Voting Ensemble, KMeans, and Label Propagation.
* **Class Imbalance Handling**: Option-level training is highly imbalanced (3 distractors for every 1 correct answer). All supervised models are initialized with `class_weight='balanced'`.
* **Evaluation Metrics**: 
  * **Classification**: Precision, Recall, F1-Score per class (evaluated via Classification Report and Confusion Matrices).
  * **Text Generation**: BLEU, ROUGE (1 and L), and METEOR scores are used to measure how closely the generated question matches the ground-truth question.

### Model B: Distractor & Hint Generation
* **Distractor Pipeline**: Uses Logistic Regression over One-Hot Encoding (OHE) cosine similarity and word frequency features to identify plausible but incorrect sentences from the passage.
* **Word2Vec (Optional)**: Uses `word2vec-google-news-300` to find semantic neighbors to the answer that do not appear in the text.
* **Hint Pipeline**: Rule-based Bag-Of-Words overlap scored by Logistic Regression (evaluated via R^2).

## 3. Current File Structure
```
race_rc_project/
├── data/
│   ├── raw/                 # Contains ONLY train.csv
│   └── processed/           # Contains generated .pkl feature matrices
├── models/
│   ├── model_a/traditional/ # Pickled verification models
│   └── model_b/traditional/ # Pickled distractor/hint models & Word2Vec
├── src/
│   ├── preprocessing.py     # Loads train.csv, creates 80/10/10 split & OHE features
│   ├── model_a_train.py     # Trains Model A (uses balanced class weights)
│   ├── model_b_train.py     # Trains Model B (uses balanced class weights)
│   ├── inference.py         # Unified predict() API combining Model A and B
│   └── evaluate.py          # Runs BLEU/ROUGE/METEOR and classification reports
├── notebooks/
│   ├── EDA.ipynb            # Exploratory Data Analysis
│   └── experiments.ipynb    # GPU-accelerated training script (for Colab)
├── tests/
│   └── test_inference.py    # Unit tests for tokenization/templates
├── ui/
│   ├── app.py               # Streamlit application (4 screens)
│   └── components/          # Helper UI components
├── report/
│   └── final_report.md      # Template for the final PDF submission
├── .gitignore               # Ignores large data/models to prevent push failures
└── requirements.txt         # Dependencies (includes nltk, rouge-score, scikit-learn, etc.)
```

## 4. Key Fixes Recently Applied (Do NOT revert these)
1. **Removed `cuml.semi_supervised`**: Colab RAPIDS/cuML does not actually support Label Propagation. It was replaced with `sklearn.semi_supervised.LabelPropagation` and runs on the CPU.
2. **Removed `.toarray()`**: The verification features generated by `preprocessing.py` are dense numpy arrays (`ndarray`), NOT sparse matrices. Calling `.toarray()` on them causes `AttributeError`. It has been completely stripped from the codebase.
3. **Fixed Colab CUDA Environment**: Changed pip installs in the Colab notebook to use `cuml-cu12` and `cudf-cu12` because Colab defaults to CUDA 12. `cupy-cuda11x` was removed as it clashes with built-in memory allocators.
4. **Git Large File Restrictions**: The `.pkl` files and `w2v.kv` are massive. They are aggressively ignored in `.gitignore` to prevent `fatal: remote end hung up` errors during GitHub pushes.
5. **Directory Creation in Colab**: `joblib.dump` fails if parent directories don't exist in Google Drive. Added `os.makedirs(..., exist_ok=True)` directly in the notebook to auto-create `models/model_a/traditional/` and `models/model_b/traditional/` inside Drive before saving.

## 5. Current Hybrid Execution Workflow
If the user asks how to run this project, instruct them to follow this exact pipeline:
1. **Local**: Push code to GitHub. Upload `data/processed/*.pkl` to Google Drive.
2. **Colab**: Open `notebooks/experiments.ipynb` directly from GitHub inside Colab. Run on a T4 GPU. The notebook reads data from Google Drive and saves models (`svm_gpu.pkl`, `kmeans.pkl`) directly back to Google Drive.
3. **Local**: Download the trained models from Drive to the local `models/` folder. Run `src/model_a_train.py` and `src/model_b_train.py` locally for the CPU models.
4. **Local Eval & UI**: Run `python src/evaluate.py` to calculate NLP metrics and `streamlit run ui/app.py` for the web app.
